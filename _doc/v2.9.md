# v2.9 - Memory Engine 持久化

## 完成日期
2026-05-16

## 完成項目

### HashMemoryEngine 持久化
- [x] open(path) - 從 hashtable.dat 讀取資料
- [x] flush() - 將資料寫入 hashtable.dat
- [x] sync() - 將資料寫入 hashtable.dat
- [x] 測試：test_hash_persistence

### BTreeMemoryEngine 持久化
- [x] open(path) - 從 btree.dat 讀取資料
- [x] flush() - 將資料寫入 btree.dat
- [x] sync() - 將資料寫入 btree.dat
- [x] 測試：test_btree_persistence

### 更新範例
- [x] examples/memory_engine.rs - 新增 Hash/BTree 持久化測試

## API 使用

```rust
// HashMemoryEngine 持久化
let mut engine = HashMemoryEngine::open("/data/myapp")?;
engine.put(1, b"key", b"value")?;
engine.flush()?;  // 寫入磁碟

// reopen - 資料正確讀回
let engine = HashMemoryEngine::open("/data/myapp")?;
let val = engine.get(1, b"key")?;  // 回傳 Some(value)

// BTreeMemoryEngine 持久化 (相同用法)
let mut engine = BTreeMemoryEngine::open("/data/myapp")?;
```

## 引擎限制更新

| 功能 | HashMemory | BTreeMemory | BTree | Lsm |
|------|-------------|-------------|-------|-----|
| new() | 純記憶體 | 純記憶體 | 純記憶體 | 純記憶體 |
| open(path) | ✅ 檔案 | ✅ 檔案 | ✅ | ✅ |
| flush() | ✅ | ✅ | ✅ | ✅ |

## 技術細節

- 使用 bincode 序列化/反序列化
- 每次 open 從磁碟讀取資料重建 in-memory 結構
- 不依賴 mmap 位址，重啟後資料正確

## 下一步 (v2.10)

- 效能優化
- 更多 SQL 功能