# v4.2 Queue 強化

## 完成日期
2026-05-17

## 功能清單

### SyncQueue 強化

| 功能 | 說明 | 狀態 |
|------|------|------|
| QueueConfig | 設定 max_delivery_count, dlq_name, message_ttl_secs, priority_enabled | ✅ |
| enqueue_at | 指定 deliver_at 時間延遲 delivery | ✅ |
| enqueue_delay | 指定 delay_secs 延遲 delivery | ✅ |
| enqueue_priority | 優先級訊息（需先啟用 priority_enabled） | ✅ |
| batch_enqueue | 一次 enqueue 多個訊息 | ✅ |
| DLQ | 超過 max_delivery_count 自動移至 Dead Letter Queue | ✅ |
| cleanup_expired | 依 message_ttl_secs 清理過期訊息 | ✅ |
| dlq_length | 取得 DLQ 訊息數量 | ✅ |
| purge_dlq | 清空 DLQ | ✅ |
| priority_length | 取得優先級佇列長度 | ✅ |

### AsyncQueue 強化

| 功能 | 說明 | 狀態 |
|------|------|------|
| AsyncQueueConfig | 設定 max_delivery_count, dlq_name, message_ttl_secs, priority_enabled | ✅ |
| enqueue_at | 指定 deliver_at 時間延遲 delivery | ✅ |
| enqueue_delay | 指定 delay_secs 延遲 delivery | ✅ |
| enqueue_priority | 優先級訊息（需先啟用 priority_enabled） | ✅ |
| batch_enqueue | 一次 enqueue 多個訊息 | ✅ |
| DLQ | 超過 max_delivery_count 自動移至 Dead Letter Queue | ✅ |
| cleanup_expired | 依 message_ttl_secs 清理過期訊息 | ✅ |
| dlq_length | 取得 DLQ 訊息數量 | ✅ |
| purge_dlq | 清空 DLQ | ✅ |
| priority_length | 取得優先級佇列長度 | ✅ |

## API 變更

### QueueConfig

```rust
#[derive(Debug, Clone)]
pub struct QueueConfig {
    pub max_delivery_count: u32,   // 預設 3
    pub dlq_name: Option<String>,  // DLQ 佇列名稱
    pub message_ttl_secs: Option<u64>,  // 訊息 TTL
    pub priority_enabled: bool,    // 啟用優先級
}
```

### SyncQueue 新增方法

```rust
impl SyncQueue {
    pub fn with_config(name: &str, engine: Arc<RwLock<KvEngine>>, config: QueueConfig) -> Self;
    pub fn config(&self) -> &QueueConfig;
    pub fn set_config(&mut self, config: QueueConfig);
    pub fn enqueue_at(&mut self, payload: Vec<u8>, visibility_timeout: u64, deliver_at: u64) -> Result<String>;
    pub fn enqueue_delay(&mut self, payload: Vec<u8>, visibility_timeout: u64, delay_secs: u64) -> Result<String>;
    pub fn enqueue_priority(&mut self, payload: Vec<u8>, priority: u8) -> Result<String>;
    pub fn batch_enqueue(&mut self, payloads: Vec<Vec<u8>>, visibility_timeout: u64) -> Result<Vec<String>>;
    pub fn cleanup_expired(&mut self) -> Result<usize>;
    pub fn dlq_length(&self) -> Result<usize>;
    pub fn purge_dlq(&mut self) -> Result<usize>;
    pub fn priority_length(&self) -> Result<usize>;
}
```

### AsyncQueue 新增方法

```rust
impl AsyncQueue {
    pub fn with_config(name: &str, config: AsyncQueueConfig) -> Self;
    pub fn config(&self) -> &AsyncQueueConfig;
    pub fn set_config(&mut self, config: AsyncQueueConfig);
    pub async fn enqueue_at(&mut self, payload: Vec<u8>, visibility_timeout: u64, deliver_at: u64) -> Result<String, String>;
    pub async fn enqueue_delay(&mut self, payload: Vec<u8>, visibility_timeout: u64, delay_secs: u64) -> Result<String, String>;
    pub async fn enqueue_priority(&mut self, payload: Vec<u8>, priority: u8) -> Result<String, String>;
    pub async fn batch_enqueue(&mut self, payloads: Vec<Vec<u8>>, visibility_timeout: u64) -> Result<Vec<String>, String>;
    pub async fn cleanup_expired(&mut self) -> Result<usize, String>;
    pub async fn dlq_length(&self) -> Result<usize, String>;
    pub async fn purge_dlq(&mut self) -> Result<usize, String>;
    pub async fn priority_length(&self) -> Result<usize, String>;
}
```

## 訊息結構變更

SyncQueueMessage 和 AsyncQueueMessage 新增 priority 欄位：

```rust
pub struct SyncQueueMessage {
    pub id: String,
    pub payload: Vec<u8>,
    pub enqueued_at: u64,
    pub delivery_count: u32,
    pub visibility_timeout: u64,
    pub visible_after: u64,
    pub priority: u8,        // 新增
    pub metadata: Option<String>,
}
```

## 錯誤類型變更

MsgqError 新增 InvalidOperation variant：

```rust
#[derive(Error, Debug)]
pub enum MsgqError {
    // ... existing variants ...
    #[error("Invalid operation: {0}")]
    InvalidOperation(String),
}
```

## 測試

- 所有 166 tests 通過
- 新增功能已驗證可正常運作

## 相關檔案

- `src/msgq/sync_queue.rs`
- `src/msgq/async_queue.rs`
- `src/msgq/message.rs`
- `src/msgq/error.rs`
- `src/msgq/mod.rs`